// Map method it create a new array

const array = [2, 3, 4, 5, 6];
const sqare = function (number) {
  return number * number;
};
const myNewArray = array.map(sqare);
console.log(myNewArray);
